apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ex288-php-mysql.fullname" . }}-test-connection"
  labels:
    {{- include "ex288-php-mysql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ex288-php-mysql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
