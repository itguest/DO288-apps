apiVersion: v1
kind: Service
metadata:
  name: {{ include "ex288-php-mysql.fullname" . }}
  labels:
    {{- include "ex288-php-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ex288-php-mysql.selectorLabels" . | nindent 4 }}
